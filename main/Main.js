const TERMINALHEIGHT = 200;
function setup(){
var playground = createCanvas(windowWidth, windowHeight-TERMINALHEIGHT);
    playground.parent("Playground");
var terminal = createCanvas(windowWidth,TERMINALHEIGHT)
    terminal.parent("Terminal");
background(0);   
}

function draw(){
  //TODO  
}